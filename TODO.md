# TODO（スコープ外で気づいた改善点の置き場）

- 生涯ステージ（endlessStage の称号）が変わったときの祝福演出は未実装
  （今回のレベルアップ祝福は12段階の成長ステージのみ対象）。
- app.js が 1.18MB（gzip 207KB）。code-splitting は未着手（rolldown の
  codeSplitting オプションで改善余地）。
- OCR取り込み（ImportSheet）は実スクショで精度不足のまま保留中。口数×基準価額
  モードが実質の置き換え。
- オルコギへの機能移植が大きく遅れている（sleep collection 以降の全機能）。
- 曜日イベント（「◯曜日は◯◯の日」）は未実装。明日の予告のネタ源として相性が良い。
- animLevel="min"（ひかえめモード）では更新ループが回らないため、芸（お手など）を
  実行するとポーズが解除されず固まる（今回のバッチ以前からの既存挙動。
  CompanionStage.tsx の `if (isMin) return;` が fsm 遷移ごと止めているのが原因。
  min でも fsm 時間経過だけは進める修正が必要）。
